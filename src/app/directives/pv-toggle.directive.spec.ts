import { PvToggleDirective } from './pv-toggle.directive';

describe('PvToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new PvToggleDirective();
    expect(directive).toBeTruthy();
  });
});
